// Shared easing tuple. Typed as a fixed 4-tuple so framer-motion accepts it
// as a cubic-bezier definition (a plain number[] is rejected by its types).
export const EASE: [number, number, number, number] = [0.16, 1, 0.3, 1];
